# Review of v0.1.1-outline
Reviewer: Ontology
Date: 2026-09-06
Verdict: PASS

**Version read:** `public-papers/observer-space-framework/versions/v0.1.1-outline.md` on `main` (PR #83). Prior HOLD: `reviews/v0.1-outline-ontology.md`.

Re-check of Ontology HOLD items 1–4 only. Does not demand Born. Does not rewrite the outline. Does not restart leftover R-stack.

---

## Verdict

**PASS.** The four label HOLDs from v0.1 are cleared. Prose expansion of §§2–5 may proceed on this ontology footing. Remaining items below are **NITS**, not blockers.

---

## HOLD 1 — $O$ vs $O^u$ for EPP / $B$ — **cleared**

§2 public convention: Paper 1 lock stated; bare $O$ = jet ≡ simply-connected patch; $O^u = (O,u)$; **$B$ and EPP1 are about $O^u$**; write $B(O^u)$; bare $O$ when orientation is not yet chosen. §5 retitled $B(O^u)$ and forbids ambiguous $B(O)$ for the EPP story. §7 EPP1 is on arms of $B(O^u)$.

---

## HOLD 2 — lump / gauge as Paper 1 jet ≡ patch — **cleared**

§2: public object is jet ≡ simply-connected analytic patch; gauge / lump identity = pointed analytic diffeomorphism renaming the same patch; public object is **not** a raw coordinate jet with gauge deferred forever; lump-quotient details in-house. Matches Paper 1 option (a).

---

## HOLD 3 — $E_W$ / $E_M$ relative, not second observers / no whole-$W$ from $O$ — **cleared**

§2: $E_W$ / $E_M$ named as **relative ensembles**, not a second elementary observer; not a vote of global topology on splits; **no reconstruction of a whole $W$ from $O$**. Sibling points = ensemble multiplicity, not a fork of the elementary object. Abstract bullet matches.

---

## HOLD 4 — §6 4-geons as picture only — **cleared**

§6 retitled “optional picture — not ontology.” Bullet: picture / cartoon only; not Paper 1 lock; does not adopt 4-geons as definition of $O$; topology-as-type is picture or later hope, not §2. §10 repeats: 4-geons are picture, not lock-side ontology.

---

## Firewall / smuggle table (from prior review) — status

| Item | v0.1.1 |
|---|---|
| No Born / no $|a|^2$ multiplicity | Kept (top firewall, §8, §10) |
| EPP1 as postulate, not ontology | Cleared (§7) |
| EPP2 / product ansatz as hope | Cleared (§7) |
| 4-geons not ontology | Cleared (§6, §10) |
| Not decoherence / Everett–DW frame | Cleared (firewall, §8, §10) |
| Not Stoica | Cleared (§7, §10) |
| Not Gielen–Wise / not Larsson | Cleared (§2, §10) |
| Analytic, not smooth | Kept |
| Discrete $B$ hard gate for EPP | Cleared (§5) |

---

## Remaining items (NITS — non-blocking)

1. §1 still has one bullet “Define branches $B(O)$…” while the EPP story is $B(O^u)$. Harmless if §2/§5 govern; tidy when expanding.
2. §5 stress test still writes $O^*$ / $|B(O^*)|$ without $u$. Prefer $O^{u*}$ / $B(O^{u*})$ for consistency with §6’s $O^{u*}$.
3. §9 open bullet still says “in v0.1” for which object $O$ is; bump to v0.1.1 when touching that line.

None of these reopens HOLD 1–4.

---

## Postulate / hope / open (unchanged stance)

- **Postulate:** CWS; EPP1 for narrative (labeled).
- **Hope:** Born; Einstein; product ansatz / EPP1⇔EPP2; exhaustion.
- **Open:** $T_O$ on $\mathrm{Obs}$; discrete $B(O^u)$ under CWS; compatibility transitive closure; analytic vs small-ball tension (object currently analytic jet/patch).

---

## Next

Ontology clears HOLD on v0.1.1. Sitting unless David or Chief of Staff sends another job. Leftover R-stack still ceased.
